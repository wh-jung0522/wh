<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leaddev.whstudy.mapper.UserMapper">

    <!-- 매칭 -->
    <select id="matchUser" resultType="Boolean">
        SELECT COUNT(*) = 1 FROM user WHERE UserID = #{userID}
    </select>
    <!--  로그인  -->
    <select id="loginUser" resultType="Boolean">
        SELECT
        IF(COUNT(UserName) = 1,1,0)
        FROM user
        WHERE
        UserID = #{userID} AND HashedUserPW = #{hashedUserPW}
    </select>

    <!--  생성  -->
    <insert id="createUser">
        INSERT INTO user(UserID, HashedUserPW, UserName)
        VALUES (#{userID}, #{hashedUserPW}, #{userName})
    </insert>
    <!--  수정  -->
    <update id = "modifyUser">
        UPDATE USER
        SET
        HashedUserPW = #{hashedUserPW}
        ,UserName = #{userName}
        WHERE
        UserID = #{userID}
    </update>
    <!--  삭제  -->
    <delete id="deleteUser">
        DELETE FROM user
        WHERE
        UserID = #{userID}
    </delete>
</mapper>
